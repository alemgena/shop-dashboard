import React from 'react'
import RanchManager from './newRanchManager'
export const RanchManagerPageConfig={
  routes: [
    {
      path: "/ranchManager",
      exact: true,
      component: RanchManager
    }
  ]
}

